import { useDispatch } from 'react-redux'
import './Menu.css'

const Menu = () => {

  const dispatch = useDispatch()

  return (
    <div className="Menu">
      este es el menu
    </div>
  )
}

export default Menu
